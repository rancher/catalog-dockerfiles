[
  {rabbit, [
    {{if (getenv "RABBITMQ_CLUSTER_PARTITION_HANDLING")}}{cluster_partition_handling, {{getenv "RABBITMQ_CLUSTER_PARTITION_HANDLING"}}},{{end}}
    {cluster_nodes,
        {[
{{range $idx, $cnt := gets "/self/service/containers/*"}}{{ if (ne $idx 0) }},{{end}} 'rabbit@{{.Value}}'{{end}}
         ], disc}     },
  {loopback_users, []}
  ]},
  {kernel, [
    {{if (getenv "RABBITMQ_NET_TICKTIME")}}{net_ticktime, {{getenv "RABBITMQ_NET_TICKTIME"}}},{{end}}
  ]}
].
