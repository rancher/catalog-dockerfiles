[
  {rabbit, [
    {cluster_nodes,
        {[
{{range $idx, $cnt := gets "/self/service/containers/*"}}{{ if (ne $idx 0) }},{{end}} 'rabbit@{{.Value}}'{{end}}
         ], disc}     },
  {loopback_users, []}
  ]
}].

